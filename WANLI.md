# 浙江万里学院适配

本分支只服务 SunrayV2 标签 **`浙江万里学院发货`**（`8fcd229e`）及其钉死的 yunlink（`cb519eb`）。

不要用本包去连 main 线上的新仿真；新客户请用 `main` 和 Release `matlab-1.1.0`。

MATLAB 用户包：GitHub Release `matlab-wanli`。安装步骤与 main 相同：解压 zip，双击 `.mltbx`，运行 `ex00_setup`。
