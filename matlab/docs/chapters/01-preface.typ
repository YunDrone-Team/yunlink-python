#import "../vendor/mantys/src/mantys.typ": *

= 前言

== 本文档的用途

YunLink Sunray MATLAB Support（以下简称本工具箱）使 MATLAB 能够通过本机 Python 与局域网中的 YunLink Bridge 通信，从而读取 Sunray 无人机与无人车的状态，并在条件允许时发送控制指令。

本文是本工具箱的*使用说明书*。阅读对象为首次在 MATLAB 中使用本工具箱的人员。即使您尚未熟悉 MATLAB 的命令窗口或从未编写过脚本，亦可按本文逐步完成安装与第一次运行。

与本文配套的还有工具箱内的 `examples/` 目录。二者分工如下：

- *本文*：说明每一项操作的前提、位置、输入与预期结果。
- *示例脚本*：可直接运行的 MATLAB 代码。脚本内的注释与本文一致。建议按编号运行示例，需要查阅函数时再打开第 8 章。

== 建议阅读顺序

+ 第 2 章：安装并启动 MATLAB，安装匹配的 Python。
+ 第 3 章：下载发布包并安装 `.mltbx`。
+ 第 4 章：运行 `yunlink_examples` 与 `ex00_setup`，填写 `yunlink.env`。
+ 第 5 章：理解搜索所用的 UDP 9697 与连接所用的 TCP 9696。
+ 第 6 章：按编号运行 `ex00` 至 `ex12`。
+ 第 7、8 章：在能够独立编写脚本时查阅连接模型与函数参考。
+ 第 9 章：出现报错时按现象检索。

== 约定

- 本文以「您」称呼读者。
- *须*表示不做则无法继续；*请*表示当前步骤；*建议*表示可选。
- 在命令窗口中输入函数名，例如 `ex01_discover`（不含 `.m` 后缀）。
- 设备标识使用 `entity_uid`，例如 `e-f97f96-2-1`。目录表中的显示名（如 `uav1`）仅供阅读。
- 数字均带单位。`yunlink_translate` 的第三个参数是速度（米/秒），第四个参数是持续时间（秒），二者相乘才是大约位移。

#info-alert[
  本工具箱的通信对端为正在运行的 *YunLink Bridge*（仿真进程或机载通信进程）。MATLAB 通过 Bridge 搜索设备、建立会话并收发指令。
]

#warning-alert[
  `ex04`、`ex05`、`ex06`、`ex12` 以及 `basic_flight_demo` 会发出真实控制。请仅在仿真环境或已清空周围障碍的场地运行，并确认现场人员已知晓。
]
